name: Shelf-safe sync boundary

on:
  pull_request:
    paths:
      - "scripts/sync-ui.py"
      - "scripts/instance_identity.py"
      - "tests/test_sync_ui.py"
      - ".github/workflows/shelf-sync-boundary.yml"
  push:
    branches:
      - main
    paths:
      - "scripts/sync-ui.py"
      - "scripts/instance_identity.py"
      - "tests/test_sync_ui.py"
      - ".github/workflows/shelf-sync-boundary.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify Shelf-safe sync ownership
        run: python3 -m unittest discover -s tests -p 'test_*.py'
