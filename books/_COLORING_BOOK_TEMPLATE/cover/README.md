# Cover source

This directory holds source artwork and provenance, not a page-count-dependent final wrap.

Keep generated source art free of title, author, ISBN, barcode, trim marks, fold guides, and fake endorsements. The final composition should place typography and platform-specific geometry from edition data after the page count and target profile are known.
